import "./globals.css"
export const metadata = {
  title: "Amazin Cyber — Lead Pipeline",
  description: "CRM pipeline for Amazin Cyber leads",
}
export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>
}
